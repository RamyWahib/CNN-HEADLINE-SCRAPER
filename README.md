STEP 1: Install the Dependencies

Install the files
Cd to the appropriate directory on the command prompt
Execute the command "pip install -r requirements.txt"

STEP 2: Run the Python Script


STEP 3: Run the MATLAB Script
